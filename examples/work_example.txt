if (m==2) {
    k += 3 + 4;
    k = 3 + 4;
} else if (m==n)
    m -= 2 + 3;
else {
    n *= n + 2;
}
n /= 2 + 3;

for a = 3:4
    while (2 < 3) {
        print 4, "kkl";
        break;
    }

l = -3;

return 7;

M = ones(3)';
M = zeros(n)'';
M = eye(3 + 4);
M = [ [2, 3, 4], [4, 5, 6] ];
M = A .+ [ [2, 3] ]; # some comment

return;

M[2+4-n, 3:10] = ones(3) .+ zeros(n/4);
M[2:2] = 0;
M[2, 3] = 4;